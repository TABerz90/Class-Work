DECLARE String, Rstring : String
DECLARE Counter : Integer
OUTPUT "Please enter text:"
INPUT String
Counter <--- len(String) - 1
FOR Counter T0 0:
    Rstring <--- Rstring + String(Counter)
ENDFOR
IF String = Rstring THEN
    OUTPUT "Palindrome"
ELSE
    OUTPUT "Not Palindrome"
ENDIF
